<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - NIT Jalandhar Mentor Connect</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-blue-900 text-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4">
            <div class="flex justify-between items-center py-4">
                <div class="flex items-center space-x-4">
                    <h1 class="text-xl font-bold">NIT Jalandhar</h1>
                    <span class="text-blue-200">Mentor Connect</span>
                </div>
                <div class="flex items-center space-x-4">
                    <% if (typeof user !== 'undefined' && user) { %>
                        <a href="<%= user.isAdmin ? '/admin/dashboard' : '/user/dashboard' %>" class="hover:text-blue-200">
                            <i class="fas fa-home mr-1"></i>Dashboard
                        </a>
                    <% } else { %>
                        <a href="/" class="hover:text-blue-200">
                            <i class="fas fa-home mr-1"></i>Home
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto px-4 py-16">
        <div class="text-center">
            <div class="mb-8">
                <i class="fas fa-exclamation-circle text-8xl text-red-500 mb-4"></i>
                <h1 class="text-4xl font-bold text-gray-800 mb-4">Oops! Something went wrong</h1>
                <p class="text-xl text-gray-600 mb-8">
                    <%= message || 'An unexpected error occurred. Please try again later.' %>
                </p>
            </div>
            
            <div class="flex justify-center space-x-4">
                <button onclick="history.back()" 
                        class="bg-gray-600 text-white px-6 py-3 rounded-lg hover:bg-gray-700 transition duration-300 inline-flex items-center">
                    <i class="fas fa-arrow-left mr-2"></i>Go Back
                </button>
                <a href="/" 
                   class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition duration-300 inline-flex items-center">
                    <i class="fas fa-home mr-2"></i>Go Home
                </a>
            </div>
        </div>
    </main>
</body>
</html>